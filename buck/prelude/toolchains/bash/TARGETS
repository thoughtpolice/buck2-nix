load("@prelude//nixpkgs.bzl", "nix")

nix.rules.build(
    name = "oil",
    expr = "pkgs.oil",
    visibility = [ "root//..." ],
)

nix.rules.build(
    name = "bash",
    expr = "pkgs.bash",
    visibility = [ "root//..." ],
)
